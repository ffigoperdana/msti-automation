# Server Configuration
NODE_ENV=development
PORT=3000

# Database PostgreSQL Eksternal
DATABASE_URL=postgres://username:password@host:port/database

# JWT Secret untuk autentikasi (jika digunakan)
JWT_SECRET=rahasia_jwt_anda

# InfluxDB (opsional, jika digunakan dalam model DataSource)
# Catatan: Ini bukan koneksi wajib, karena DataSource di aplikasi
# memungkinkan pengguna menambahkan sumber data InfluxDB melalui UI/API 